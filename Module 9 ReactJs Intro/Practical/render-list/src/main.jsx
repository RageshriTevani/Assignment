import React from 'react'
import ReactDOM from 'react-dom/client'
import ListView from './Renderlist'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ListView/>
  </React.StrictMode>,
)
